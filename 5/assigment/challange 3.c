#include <stdio.h>  // Includes the standard input/output library for printf function

int main() {        // Entry point of the program - execution starts here
    printf("Name: Alex\n");      // Prints the name followed by a newline using %s implicitly for string
    printf("Age: 20\n");         // Prints the age as an integer literal followed by a newline
    printf("Country: India\n");  // Prints the country string followed by a newline
    
    return 0;       // Indicates successful program completion to the operating system
}
