def my_map(func, iterable):
    for item in iterable:
        yield func(item)

# Tests
print(list(my_map(lambda x: x*2, [1, 2, 3])))  # [2, 4, 6]
print(list(my_map(str.upper, "hello")))         # ['H', 'E', 'L', 'L', 'O']
